import { IApplicationState } from '@/store/types'

import { TPostsActions } from '@/store/posts/types'

export const postReducer = (state: IApplicationState, action: TPostsActions): IApplicationState => {
  return state
}
